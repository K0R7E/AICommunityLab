# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: canonical browser origin for OAuth redirects and auth callbacks
# (avoids trusting Host behind proxies). Example: https://yourdomain.com
# NEXT_PUBLIC_SITE_URL=
